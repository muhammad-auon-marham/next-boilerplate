# API origin. Leave empty so the app calls relative `/api/...` URLs (Launchpad routes `/api` to the backend).
# For local development without Launchpad, point it at the backend, e.g. http://localhost:3000
NEXT_PUBLIC_SERVER_URL=

# Public resource directory
NEXT_PUBLIC_ASSETS_DIR=
